{
  "name": "flashui-cyber",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "dev:full": "concurrently -n \"DEV,TYPE,MONITOR\" -c \"bgBlue,bgMagenta,bgGreen\" \"npm run dev\" \"npm run type-check:watch\" \"npm run dev:monitor\"",
    "type-check": "tsc --noEmit",
    "type-check:watch": "tsc --noEmit --watch",
    "dev:monitor": "echo '✅ Cyber-UI Development Environment Active'",
    "setup": "npm install && echo '✅ Dependencies installed! Run: npm run dev:full'",
    "start:all": "npm run setup && npm run dev:full"
  },
  "dependencies": {
    "@google/genai": "^0.7.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "concurrently": "^9.1.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
